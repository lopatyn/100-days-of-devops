# Глибокий розбір: Поведінка Nginx при закінченні терміну дії SSL-сертифіката 🧠🔒

Під час Day 15 ми обговорювали критичне питання "Knowledge Check" про те, що станеться, коли термін дії сертифіката закінчиться. Цей документ надає детальний технічний розбір того, як Nginx та клієнти обробляють прострочені сертифікати.

## 1. Сторона сервера (Nginx)
Коли Nginx запускається або перезавантажується, він зчитує файли сертифікатів з диска та завантажує їх у **RAM (оперативну пам'ять)**.

- **Постійність:** Nginx **не перевіряє** дату закінчення терміну дії сертифіката для кожного вхідного запиту. Він просто віддає ті байти, які зараз зберігаються в його пам'яті.
- **Статус сервісу:** Якщо термін дії сертифіката закінчується опівночі, Nginx **не впаде** і **не зупинить** сервіс. Він продовжуватиме обслуговувати прострочений сертифікат, поки ви вручну не перезапустите або не перезавантажите його (`nginx -s reload`).

## 2. Сторона клієнта (Справжній вартовий)
Відповідальність за валідацію сертифіката лежить виключно на **клієнті** (браузер, `curl`, Jump Host тощо).

- **Перевірка під час Handshake:** Під час **TLS Handshake** клієнт отримує сертифікат і перевіряє поле `Not After`.
- **Порівняння часу:** Клієнт порівнює дату закінчення терміну дії сертифіката зі своїм системним годинником. Якщо годинник хоча б на одну секунду випереджає цей ліміт, клієнт негайно розірве з'єднання.
- **Приклади помилок:**
    - **curl:** `SSL certificate problem: certificate has expired`
    - **Браузери:** `NET::ERR_CERT_DATE_INVALID`

## 3. Сценарій "Північ" (Активні з'єднання)
Що відбувається з користувачами, які вже підключені в момент закінчення терміну дії сертифіката?

- **Встановлені сесії:** Якщо TLS-сесія вже встановлена (наприклад, триває завантаження великого файлу), вона **не буде** перервана. Перевірка валідності сертифіката відбувається лише на етапі **Handshake**.
- **Нові сесії:** Будь-яка спроба ініціювати **новий** handshake після моменту закінчення терміну дії буде відхилена клієнтом.

## 4. Найкращі оперативні практики
- **Моніторинг:** Завжди відстежуйте стан сертифікатів за допомогою таких інструментів, як Prometheus Blackbox Exporter або автоматизованих скриптів, які перевіряють термін дії через `openssl s_client`.
- **Логіка ротації:** При оновленні файлу сертифіката на диску ви **обов'язково** повинні перезавантажити Nginx (`systemctl reload nginx`). Без перезавантаження Nginx продовжуватиме тримати в пам'яті старий (і, можливо, вже прострочений) сертифікат.
- **Безпечне перезавантаження:** Використання `nginx -t && systemctl reload nginx` — це найбезпечніший спосіб ротації сертифікатів без розриву активних з'єднань.

---
**Основна документація:** [Day 15 Summary](./README.md)
