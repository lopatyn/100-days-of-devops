# Глибокий розбір: Захист та Відмовостійкість LBR 🛡️⚖️

## 1. Обмеження запитів за допомогою `limit_req`

У виробничому середовищі ваш балансувальник навантаження — це перша лінія захисту від DDoS-атак на рівні додатків або помилок у конфігурації клієнтів. Модуль Nginx `limit_req` використовує алгоритм **"Leaky Bucket"** (діряве відро) для запобігання перевантаженню бекендів.

### Розбір конфігурації
Для впровадження обмеження швидкості ви спочатку визначаєте зону спільної пам'яті для зберігання станів, а потім застосовуєте її до конкретних локацій.

```nginx
# Визначається в контексті 'http'
limit_req_zone $binary_remote_addr zone=mylimit:10m rate=5r/s;

server {
    location / {
        # burst: дозволяє короткочасні сплески до 10 запитів
        # nodelay: обробляє burst-запити негайно (корисно для плавного UX)
        limit_req zone=mylimit burst=10 nodelay;
        
        proxy_pass http://stapp;
    }
}
```

- **`$binary_remote_addr`**: Використовує IP клієнта в бінарному форматі (займає лише 32 байти для IPv4), що набагато економніше для пам'яті, ніж рядковий `$remote_addr`.
- **`5r/s`**: Відхиляє запити, якщо вони перевищують 5 на секунду з однієї IP-адреси.
- **Чому це важливо:** Це змушує зловмисників використовувати більше IP-адрес (розподілена атака) та захищає пули з'єднань бекенда від вичерпання одним шкідливим актором.

---

## 2. Усунення єдиної точки відмови (SPOF)

Сам балансувальник навантаження має бути високонадійним. Якщо вузол LBR вийде з ладу, весь стек стане недоступним.

### Порівняння архітектур

| Функція | On-Premise (Залізо/VMs) | Cloud-Native (AWS/Azure/GCP) |
| :--- | :--- | :--- |
| **Інструменти** | **Keepalived** + VRRP | Managed LB (напр. **AWS ALB**) |
| **Логіка** | Два LBR розділяють **віртуальну IP (VIP)**. | LB розподілений між декількома **зонами доступності**. |
| **Failover** | Резервний вузол миттєво забирає VIP, якщо Master "падає". | DNS вказує на декілька IP, керованих хмарним провайдером. |
| **Складність** | Ручне налаштування "пульсу" та скриптів перемикання. | Принцип "налаштував і забув" з вбудованими перевірками здоров'я. |

### White Hat Challenge: Обхід через "Прямий доступ" 🕵️‍♂️
Захист на рівні LBR марний, якщо зловмисник може звернутися до App-серверів напряму.
1. **Ізоляція мережі:** Сервери додатків мають приймати трафік на свої порти (напр. 8083) ТІЛЬКИ з IP-адреси LBR.
2. **Впровадження:** Використовуйте `iptables` або хмарні Security Groups, щоб скидати весь трафік на порт 8083, якщо джерелом не є LBR.

```bash
# Приклад захисного правила на stapp01
sudo iptables -A INPUT -p tcp -s 172.16.238.14 --dport 8083 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 8083 -j DROP
```

---

## 3. Найкращі практики для професійних LBR
- **Логування:** Завжди логуйте `$remote_addr` ТА заголовок `X-Forwarded-For`.
- **Пасивні перевірки (Health Checks):** Використовуйте `max_fails=3 fail_timeout=30s` у блоці `upstream`, щоб Nginx автоматично ігнорував "мертві" бекенди.
- **Таймаути:** Налаштування `proxy_connect_timeout` та `proxy_read_timeout` запобігає блокуванню робочих процесів LBR повільним бекендом.

---
🔗 **Назад до:** [Звіт за День 16](./README.md)
